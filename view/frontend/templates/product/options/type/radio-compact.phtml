<?php
/**
 * ElielWeb ProductConfigurator - Compact Radio Options
 *
 * @var $block \Magento\Framework\View\Element\Template
 * @var \Magento\Catalog\Model\Product\Option $option
 * @var \ElielWeb\ProductConfigurator\ViewModel\ProductOptions $optionsViewModel
 */

$option = $block->getData('option');
$product = $block->getData('product');
$optionsViewModel = $block->getData('options_view_model');

$values = $option->getValues() ?? [];
$optionId = $option->getOptionId();
$isSizeOption = $optionsViewModel->isSizeOption($option);
?>

<!-- Hidden input to store selected value -->
<input type="hidden"
       name="options[<?= $escaper->escapeHtmlAttr($optionId) ?>]"
       <?= $option->getIsRequire() ? 'required' : '' ?>>

<div class="radio-compact-list <?= $isSizeOption ? 'size-option' : '' ?>">
    <?php foreach ($values as $value): ?>
        <?php
        $valueId = $value->getOptionTypeId();
        $price = (float)$value->getPrice();
        $priceType = $value->getPriceType();
        ?>

        <button type="button"
                class="radio-compact-item"
                @click="selectOption(<?= (int)$optionId ?>, <?= (int)$valueId ?>, <?= (float)$price ?>); open = false"
                :class="{ 'selected': selectedOptions[<?= (int)$optionId ?>]?.valueId === <?= (int)$valueId ?> }">

            <span class="value-text">
                <?= $escaper->escapeHtml($value->getTitle()) ?>
            </span>

            <?php if ($price != 0): ?>
                <span class="value-price">
                    <?php if ($priceType === 'percent'): ?>
                        <?= $price > 0 ? '+' : '' ?><?= $escaper->escapeHtml($price) ?>%
                    <?php else: ?>
                        <?= $price > 0 ? '+' : '' ?><?= $escaper->escapeHtml($block->formatPrice($price)) ?>
                    <?php endif; ?>
                </span>
            <?php endif; ?>
        </button>
    <?php endforeach; ?>
</div>

<style>
.radio-compact-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.radio-compact-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem 1rem;
    border: 1px solid #e0e0e0;
    border-radius: 4px;
    background: #fff;
    cursor: pointer;
    transition: all 0.2s ease;
    text-align: left;
    width: 100%;
}

.radio-compact-item:hover {
    border-color: #333;
    background: #f9f9f9;
}

.radio-compact-item.selected {
    border-color: #333;
    background: #333;
    color: #fff;
}

.radio-compact-item .value-text {
    font-size: 0.875rem;
    font-weight: 500;
}

.radio-compact-item .value-price {
    font-size: 0.75rem;
    color: #666;
}

.radio-compact-item.selected .value-price {
    color: rgba(255, 255, 255, 0.8);
}

/* Size option special styling */
.radio-compact-list.size-option {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
    gap: 0.75rem;
}

.radio-compact-list.size-option .radio-compact-item {
    flex-direction: column;
    padding: 0.75rem 0.5rem;
    text-align: center;
    min-height: 60px;
    justify-content: center;
}

.radio-compact-list.size-option .value-text {
    font-weight: 600;
    font-size: 0.9rem;
}

/* Responsive */
@media (max-width: 768px) {
    .radio-compact-list.size-option {
        grid-template-columns: repeat(3, 1fr);
    }
}
</style>
