<?php
/**
 * ElielWeb ProductConfigurator - Compact Select/Dropdown Options
 *
 * @var $block \Magento\Framework\View\Element\Template
 * @var \Magento\Catalog\Model\Product\Option $option
 * @var \ElielWeb\ProductConfigurator\ViewModel\ProductOptions $optionsViewModel
 */

$option = $block->getData('option');
$product = $block->getData('product');
$optionsViewModel = $block->getData('options_view_model');

$values = $option->getValues() ?? [];
$optionId = $option->getOptionId();
?>

<!-- Hidden input to store selected value -->
<input type="hidden"
       name="options[<?= $escaper->escapeHtmlAttr($optionId) ?>]"
       <?= $option->getIsRequire() ? 'required' : '' ?>>

<div class="select-compact-list">
    <?php foreach ($values as $value): ?>
        <?php
        $valueId = $value->getOptionTypeId();
        $price = (float)$value->getPrice();
        $priceType = $value->getPriceType();
        ?>

        <button type="button"
                class="select-compact-item"
                @click="selectOption(<?= (int)$optionId ?>, <?= (int)$valueId ?>, <?= (float)$price ?>); open = false"
                :class="{ 'selected': selectedOptions[<?= (int)$optionId ?>]?.valueId === <?= (int)$valueId ?> }">

            <span class="value-text">
                <?= $escaper->escapeHtml($value->getTitle()) ?>
            </span>

            <?php if ($price != 0): ?>
                <span class="value-price">
                    <?php if ($priceType === 'percent'): ?>
                        <?= $price > 0 ? '+' : '' ?><?= $escaper->escapeHtml($price) ?>%
                    <?php else: ?>
                        <?= $price > 0 ? '+' : '' ?><?= $escaper->escapeHtml($block->formatPrice($price)) ?>
                    <?php endif; ?>
                </span>
            <?php endif; ?>
        </button>
    <?php endforeach; ?>
</div>

<style>
.select-compact-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    max-height: 300px;
    overflow-y: auto;
    padding: 0.5rem;
    border: 1px solid #f0f0f0;
    border-radius: 4px;
}

.select-compact-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem 1rem;
    border: 1px solid #e0e0e0;
    border-radius: 4px;
    background: #fff;
    cursor: pointer;
    transition: all 0.2s ease;
    text-align: left;
    width: 100%;
}

.select-compact-item:hover {
    border-color: #333;
    background: #f9f9f9;
}

.select-compact-item.selected {
    border-color: #333;
    background: #333;
    color: #fff;
}

.select-compact-item .value-text {
    font-size: 0.875rem;
    font-weight: 500;
}

.select-compact-item .value-price {
    font-size: 0.75rem;
    color: #666;
}

.select-compact-item.selected .value-price {
    color: rgba(255, 255, 255, 0.8);
}

/* Scrollbar styling */
.select-compact-list::-webkit-scrollbar {
    width: 6px;
}

.select-compact-list::-webkit-scrollbar-track {
    background: #f1f1f1;
    border-radius: 4px;
}

.select-compact-list::-webkit-scrollbar-thumb {
    background: #ccc;
    border-radius: 4px;
}

.select-compact-list::-webkit-scrollbar-thumb:hover {
    background: #999;
}
</style>
